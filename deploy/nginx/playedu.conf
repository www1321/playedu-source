client_max_body_size 500m;

# PC 学员端 - 端口 9800
server {
    listen 9800;
    server_name _;
    root /opt/playedu/frontend/pc;
    index index.html;

    gzip on;
    gzip_static on;
    gzip_buffers 4 16k;
    gzip_http_version 1.1;
    gzip_comp_level 5;
    gzip_types text/plain application/javascript text/css application/xml text/javascript;
    gzip_vary on;

    location /api/ {
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_pass http://127.0.0.1:9898/;
    }

    location ~* ^/(?![api].*) {
        try_files $uri /index.html;
    }
}

# H5 移动端 - 端口 9801
server {
    listen 9801;
    server_name _;
    root /opt/playedu/frontend/h5;
    index index.html;

    gzip on;
    gzip_static on;
    gzip_buffers 4 16k;
    gzip_http_version 1.1;
    gzip_comp_level 5;
    gzip_types text/plain application/javascript text/css application/xml text/javascript;
    gzip_vary on;

    location /api/ {
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_pass http://127.0.0.1:9898/;
    }

    location ~* ^/(?![api].*) {
        try_files $uri /index.html;
    }
}

# 管理后台 - 端口 9900
server {
    listen 9900;
    server_name _;
    root /opt/playedu/frontend/admin;
    index index.html;

    gzip on;
    gzip_static on;
    gzip_buffers 4 16k;
    gzip_http_version 1.1;
    gzip_comp_level 5;
    gzip_types text/plain application/javascript text/css application/xml text/javascript;
    gzip_vary on;

    location /api/ {
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_pass http://127.0.0.1:9898/;
    }

    location ~* ^/(?![api].*) {
        try_files $uri /index.html;
    }
}
